function $(query) {
    return document.querySelectorAll(query)
}

$.first = function(query) {
    return document.querySelector(query)
}

for (const prop of ['filter', 'map', 'reduce']) {
    NodeList.prototype[prop] = Array.prototype[prop]
}